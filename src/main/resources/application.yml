
spring:
  application:
    name: 02-rest_api-app
  datasource:
    url: jdbc:mysql://mysqldb:3306/project?useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
